config {
    type: "table",
    schema: databuffet.VALIDATED_MAC5,
    dependencies: ["validated_schema_mac5", "create_all_table_raw_mac5"],
    tags: [databuffet.TAG_VALIDATED, databuffet.TAG_VALIDATED_FULL],
    uniqueKey: ["company_id", "frun", "fcode"],
    bigquery: {
        clusterBy: ["company_id", "frun", "fcode"]
    },
}

js {
    // Define table references
    const sourceTable = {
        database: databuffet.DATABASE,
        schema: databuffet.RAW_MAC5_AG01,
        name: name(),
    };

    // Build source table reference
    const sourceTableRef = `${sourceTable.database}.${sourceTable.schema}.${sourceTable.name}`;

    // Keep existing logic
    const pk_key = ["company_id", "frun", "fcode"]

    let partition_statement, post_operations_statement;

    if (pk_key && pk_key.length > 0) {
        partition_statement = `QUALIFY ROW_NUMBER() OVER(PARTITION BY ${pk_key} ORDER BY fdate desc) = 1`;
        post_operations_statement = `
BEGIN
    DECLARE retry_count INT64 DEFAULT 0;
    DECLARE max_retries INT64 DEFAULT 10;
    DECLARE success BOOL DEFAULT FALSE;

    WHILE retry_count < max_retries AND NOT success DO
        BEGIN
            ALTER TABLE ${self()} DROP PRIMARY KEY IF EXISTS;
            ALTER TABLE ${self()} ADD PRIMARY KEY (${pk_key}) NOT ENFORCED;
            SET success = TRUE;
        EXCEPTION WHEN ERROR THEN
            SET retry_count = retry_count + 1;
        END;
    END WHILE;
END;`;

    } else {
        partition_statement = "";
        post_operations_statement = "";
    }

}

WITH all_sources AS (
  SELECT 'ag01' AS company_id,
    ${databuffet.cleanString('frun')} as frun,
    ${databuffet.cleanString('fmaster')} as fmaster,
    ${databuffet.cleanString('fcode')} as fcode,
    ${databuffet.cleanString('fdate')} as fdate,
    ${databuffet.cleanString('fvehicle')} as fvehicle,
    ${databuffet.cleanString('fother')} as fother,
    ${databuffet.castInt64('fprice')} as fprice,
    ${databuffet.castInt64('fbuddy')} as fbuddy,
    ${databuffet.cleanString('fdeduct_detail')} as fdeduct_detail,
    ${databuffet.castFloat64('fdeduct_monkey')} as fdeduct_monkey,
    ${databuffet.castFloat64('fdeduct_money')} as fdeduct_money,
    ${databuffet.cleanString('ftype')} as ftype,
    ${databuffet.cleanString('fcomplete')} as fcomplete,
    ${databuffet.cleanString('foil')} as foil,
    ${databuffet.cleanString('fpass_date')} as fpass_date,
    ${databuffet.cleanString('fpay')} as fpay,
    ${databuffet.castInt64('foil_deduct')} as foil_deduct,
    ${databuffet.castInt64('loadip')} as loadip,
    ${databuffet.cleanString('loadvnos')} as loadvnos,
    ${databuffet.cleanString('fremark')} as fremark,
    ${databuffet.castFloat64('flitre_price')} as flitre_price,
    ${databuffet.castFloat64('flitre')} as flitre,
    ${databuffet.castFloat64('fcubic')} as fcubic
  FROM
    `${sourceTableRef}`
)
SELECT * FROM all_sources

${partition_statement}

pre_operations {
}

post_operations {
    ${ when(!incremental(), post_operations_statement) }
}
