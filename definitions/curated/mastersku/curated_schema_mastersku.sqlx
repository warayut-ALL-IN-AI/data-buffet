config {
    type: "operations",
    dependencies: ["validated_schema_mastersku"],
    tags: [databuffet.TAG_CURATED],
}

CREATE SCHEMA IF NOT EXISTS `${databuffet.DATABASE}.${databuffet.CURATED_MASTERSKU}` OPTIONS(location="${databuffet.REGION}") ;
